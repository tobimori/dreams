{{!< default}}
{{#author}}
<header class="featured" style="background: linear-gradient(to top, rgba(0,0,0,.2), rgba(0,0,0,.3)), url({{cover_image}}) no-repeat center / cover, #111">
  <div class="container">
    <div class="featured-container">
      {{#if location}}
      <span class="author-location">
        {{> "icons/location"}}
        From {{location}}
      </span>
      {{/if}}
      <h1 class="feature-headline">{{name}}</h1>
    </div>
  </div>
</header>

<div class="container">
  <aside class="about">
    <div class="row">
      <div class="column left">
        <h1>Hi, I'm {{name}}.</h1>
        {{#if bio}}
          <p>{{bio}}</p>
        {{/if}}
        <div class="icons">
          {{#if website}}
          <a href="{{website}}" class="icon">
            {{> "icons/web"}}
          </a>
          {{/if}}
          {{#if twitter}}
          <a href="{{twitter_url}}" class="icon">
            {{> "icons/twitter"}}
          </a>
          {{/if}}
          {{#if facebook}}
          <a href="{{facebook_url}}" class="icon">
            {{> "icons/facebook"}}
          </a>
          {{/if}}
        </div>
      </div>
      <div class="column right">
        {{#if profile_image}}
          <img class="profile-image" src="{{profile_image}}" alt="{{name}}">
        {{/if}}
      </div>
    </div>
  </section>
</div>
{{/author}}

<div class="container overlap">
  {{> "posts"}}
</div>
